# SplitTGBot

Telegram-бот для учёта совместных расходов внутри групп и личных чатов.  
Позволяет записывать траты, автоматически распределять долги между участниками и отслеживать баланс.

---

## Стек

| Технология | Назначение |
|---|---|
| C# (.NET 10) | Язык и рантайм |
| PostgreSQL | Хранение данных |
| Dapper | Работа с базой (микро-ORM) |
| Telegram.Bot | Взаимодействие с Telegram API |
| Переменные окружения | Защита секретов |

---

## Возможности

| Команда | Статус | Описание |
|---|---|---|
| /start | Готово | Регистрация пользователя в боте |
| /init | Готово | Активация бота в групповом чате (только для групп) |
| /join | В разработке | Присоединение к активной группе |
| /paid | В разработке | Запись расхода с плательщиком и описанием |
| /group | В разработке | Создание виртуальной группы в личных сообщениях |

---

## Установка и запуск

### 1. Клонирование
---
```bash
git clone https://github.com/твой-логин/SplitTGBot.git
cd SplitTGBot
```

### 2. База данных
---
Установите PostgreSQL и создайте базу (например, `splitbotdb`).
Затем выполните скрипт для создания таблиц:

```bash
psql -U postgres -d splitbotdb -f schema.sql
```

### 3. Переменные окружения
---
Задайте секреты через переменные окружения (Windows):

```cmd
setx DB_CONNECTION "Host=localhost;Port=5432;Database=splitbotdb;Username=postgres;Password=ваш_пароль"
setx BOT_TOKEN "токен_от_BotFather"
```
Важно: после выполнения команд перезапустите среду разработки.

### 4. Запуск
---
```bash
dotnet run --project SplitTGBot
```
Бот запустится и будет принимать сообщения.


## Структура проекта
---
```text
SplitTGBot.slnx
 SplitTGBot/                Консольное приложение (основной проект)
    Program.cs             Точка входа и обработка сообщений
    SplitTGBot.csproj
 TGBotClassLibrary/         Библиотека классов
    Models/                Модели данных (User, Group, ...)
    Repositories/          Репозитории для работы с БД
    MessageParser.cs       Парсер команд
 schema.sql                 SQL-схема базы данных
 .gitignore
 README.md
```

## Примечания
Безопасность: токен бота и строка подключения не хранятся в коде — только в переменных окружения. Если токен случайно попал в коммит, отзовите его через BotFather (/revoke) и задайте новый.
